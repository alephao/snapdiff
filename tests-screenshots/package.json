{
  "name": "snapdiff-screenshots",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright-driven visual-regression suite for the snapdiff web UI",
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
}
